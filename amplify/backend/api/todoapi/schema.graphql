type Todo
@model
@auth(
  rules: [
	{
	  allow: owner,
	  ownerField: "author",
	  identityClaim: "username"
	}
  ]
) {
  id: ID!
  name: String!
  description: String
  complete: Boolean!
  author: String
}
